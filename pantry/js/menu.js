$(document).ready(function () {
    // Create a jqxMenu and set its width and height.
    $("#menuBar").jqxMenu({
        width: '100%',
        height: '30px',
        theme: theme,
        minimizeWidth: null});
});